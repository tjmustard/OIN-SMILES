# v0.4.5 — status at the 2026-07-25 hard stop

All eight parallel lane agents were terminated mid-task by an account **monthly spend limit**.
Nothing is lost: every lane's work is committed to its own `swimlane/` branch, and the three
lanes that had uncommitted edits were committed as clearly-marked `WIP(...) INCOMPLETE`.

**Nothing has been merged to `main` except tooling and findings.** No encoder default changed;
every new lever ships default-OFF, so `main`'s output is unchanged from `e8b603d5`.

---

## Landed on local `main` (unpushed, per standing instruction)

| commit | what |
|---|---|
| `19d20042` | `tools/reencode_ab.py` — generator-free re-encode A/B |
| `7b85e123` | `tools/build_sweep_cohort.py` — frozen reproducible cohort builder |
| `20044883` | `tools/canonicality_probe.py` — the corrected canonicality instrument |
| `572fda1f` | `docs/RENUMBERING_INSTABILITY_v0.4.5.md` — the soundness finding |
| `d7063970` | `tools/run_sweep.sh` — parameterized, version-controlled sweep runner |

Also ready and unused: the frozen **5,000-molecule seed-42 cohort** at
`tmCAT-tmPHOTO_xyz_dataset/cohort-v0.4.5-5k` (+ `_manifest.json`), overlapping **1,319**
capstone and **790** v0.4.4-regression molecules — two identical-molecule diffable slices.

---

## Lane state — read the caveats before building on any of these

All six lane branches **trial-merge cleanly into `main`** (verified: `trial/v045-integration`).
Textual cleanliness is not semantic validation; the integrated suite run did not produce a
summary line before the stop, so **the integrated suite is UNVERIFIED**.

| lane | branch | commits | state |
|---|---|---:|---|
| 1 canonical body | `swimlane/v045-lane1` | 5 | **substantive, verification incomplete** |
| 2 canonical slots | `swimlane/v045-lane2` | 6 | **INCOMPLETE** — WIP `3722b18e` |
| 3 winding residual | `swimlane/v045-lane3` | 4 | **code complete**, doc partial |
| 4 axial | `swimlane/v045-lane4` | 2 | **substantive**, corpus scans unfinished |
| 7 research + fixtures | `swimlane/v045-lane7` | 8 | **substantive**, 3 items outstanding |
| 8 stereo stability | `swimlane/v045-lane8` | 5 | **INCOMPLETE** — WIP `8fdccb55` |
| perf | `swimlane/v045-perf` | 0 | never started |
| 5 metal Δ/Λ, 6 bound amine | — | — | never started (were blocked on Lane 2) |

### Lane 1 — canonical ligand body + canonical perception
Levers `OIN_CANONICAL_BODY`, `OIN_CANONICAL_PERCEPTION`, both default OFF. New module
`src/oinsmiles/oin/canonical_body.py` (`canonical_body`, `canonical_body_emit`).
- **Its Step-0 attribution re-scoped the release**: of the 500 capstone `rdkit_canonical` rows,
  only **104 (20.8%)** are reparse-fixable body drift; **396 (79.2%)** are `slot_or_order`, i.e.
  Lane 2's. The handoff's split was wrong.
- **The reparse alone is a measured no-op on what matters** (drift 17→17): it folds two
  *serializations* of one graph, but renumbering hands the serializer a genuinely *different
  graph*. The real fix is canonicalizing perception — two order-dependent seams, `lig_checks`
  tie resolution and `AC2BO`, the latter closed by conjugation (relabel canonically, perceive,
  map back).
- Reported A/B (250 molecules, body lever only): byte-stable 146→152, 6 fixed, 0 regressed,
  **0 key-level defects fixed** — which is what motivated the perception work.
- **Unverified:** the combined both-levers A/B, and the suite count (its last action was
  untangling two interleaved suite logs).

### Lane 2 — canonical slots (CRITICAL PATH, INCOMPLETE)
Groundwork is solid and verified: `src/oinsmiles/oin/canonical_slots.py` exposing
`lexmin_vertex_signature` (returns the argmin permutation `_polyhedron_signature` discarded) and
`canonical_slot_permutation`. **Proven behaviour-preserving**: `canonical_roundtrip_key`
recomputed over all 13,187 capstone OIN strings, SHA-256 identical before/after. 20 new tests.
Closes TD-005 (single home for the geometry vertex table).

Also pinned: **`_brute_force_symmetries` is incomplete** — for PBP it finds **2 of 10** proper
rotations, because its `[0,90,120,180,240,270]` Euler grid cannot express the 72° five-fold
rotation. The encoder currently cannot canonicalize a pentagonal-bipyramidal equatorial labeling
at all. It agrees on the other 10 geometries and never invents a non-rotation.

**The WIP half is unverified.** The agent's last words: *"DUDREA still drifts."* So the
`DUDREA_comp_0` geometry-classification flip (`[Y_SPY]`→`[Y_TET]` under renumbering) is NOT
closed, no A/B numbers exist, and the over-folding guards were not confirmed green.

### Lane 3 — winding residual: a principled refusal
`OIN_CANONICAL_ETA_WINDING`, default OFF. **Its target was wrong and it said so.** For 5 of 6
residuals the generated structure re-encodes byte-identically to the *mirrored* input, so the
question is "is mirror(M) the same compound as M?" and the answer differs per molecule:
GAMJAG and MECJOU achiral → fixed; SIMDIE, TEYXEA, WUHRIB chiral → input and generated are
**enantiomers**, correctly untouched. So `winding_star_drift` goes **6 → 2, not → 0**;
**driving it to 0 would fold enantiomers.** Four of six are the *generator* building the wrong
enantiomer, faithfully reported by the encoder.
Its first implementation *did* fold them (sorting the winding characters — the Y2 bug again);
an independent oracle (`tools/eta_core_chirality.py`) caught it. Per-fragment sense factor `eps`
is measured, not assumed: `+1` for TiCat3/4, `-1` for SIMDIE/MECJOU.

### Lane 4 — axial: overturned its own charter, and retired a trusted guard
`OIN_EMIT_AXIAL` stays default OFF (product call), now guarded by a test. Made the descriptor
perception-independent.
- **The generator DOES hold both hindered axes** (measured). The charter's premise — that it
  could not — was wrong; the descriptor could not *perceive* them on the generated side.
- **YESKOZ's two porphyrin signs are coupled**: each configuration's mirror is its exact
  complement, so `syn` and `anti` are both reflection-invariant and therefore **achiral**. The
  old `|ax:+-|` claimed chirality that does not exist — **and the 37/37 corpus mirror audit was
  confirming that false positive, not catching it**, because a complement satisfies the audit's
  translate test vacuously.
- Reconciles with Lane 7: the *relative* sign is real information, the *absolute* per-axis sign
  is not. Shipped a conservative gate dropping both (syn/anti meso-arylporphyrins now encode
  identically — a real, stated loss) and **specified but deliberately did not guess** the fix:
  canonicalize the sign vector over the automorphism orbit, `min(token, complement)` for a
  coupled pair.
- Key-folding decision recorded: unfold `_AXIAL_TOKEN_RE` in the same commit that promotes the
  lever, since folding makes the round trip structurally unable to verify what the token encodes.

### Lane 7 — fixtures + instruments
- **Both missing Lane 5 fixtures built** from a full 26,230-structure census:
  `tests/fixtures/ZUMNEC.xyz` (tris(catecholato)Mo — the *only* homoleptic tris-bidentate in the
  corpus with symmetry-equivalent donors per chelate, so it has no fac/mer distinction and metal
  helicity is its **sole** stereogenic element; mirror distinct 1.34 Å, `@OH` 11→9) and
  `tests/fixtures/JEGKOW.xyz` (4-different-donor square planar, `@SP` perm 2).
  **This closes a trap:** fac-Ir(ppy)₃'s chelates are unsymmetric (C,N), so a Lane-5 descriptor
  secretly encoding fac/mer would still pass on it. ZUMNEC fails such a descriptor.
  **Note for Lane 5:** JEGKOW's mirror is correctly *not* distinct (0.099 Å) — square planar is
  planar, so its coordination plane is a mirror plane and four different donors give
  **diastereomers, not enantiomers**. Demanding oracle-distinctness there is a category error;
  the right operator for `@SP` is a donor swap.
- **Torsion-aware configurational oracle** (`tools/injectivity/torsion_oracle.py`), 10/10 on
  cross-validation (EDOQIZ conformational 0.076; BINAP configurational 3.448 vs control 0.032).
  Building it caught two bugs that made it confidently wrong: rotatability cut the *atom* not the
  *bond* (a metal is a cut vertex, so every chelate's metal–donor bond looked rotatable), and
  automorphisms were enumerated on the H-explicit graph where methyls starve the budget
  (EDOQIZ: 4000 matches → **6** distinct heavy images vs **864** on the heavy skeleton).
- **Defect found in existing `tools/injectivity/oracle.py`**: the same automorphism starvation.
  It changes no curated fixture verdict, so nothing in `BASELINE.md` §3 moves, but it inflates
  rigid RMSD on methyl-rich species (EDOQIZ 2.55 → 0.50 Å once complete), so the rigid oracle
  **over-reports chirality at dataset scale** and `uu_hunt`'s "chiral" gate inherits it. Left
  unchanged and flagged.
- **Twin operators** `invert_stereocenter` and `swap_donor` built, plus `report.py --operators`.
  Donor-swap on real geometry: all four *cis* swaps of PtMeNH3ClBr and all six of FeH2(CO)₄ are
  distinct isomers the key **separates**; the two *trans* swaps correctly read not-distinct
  (a trans exchange is a 180° rotation — a built-in negative control).
- **Outstanding:** the 70-case UU residue split and its hand inspection, the Task-B corpus-pair
  line, and the exact suite count.

### Lane 8 — stereo stability (UNPLANNED, INCOMPLETE)
Opened in response to the renumbering finding. Merged Lane 1's perception lever as the shared
root, then added `src/oinsmiles/oin/stable_stereo.py` + a 14-line `xyz2mol.py` hook + two
regression fixtures from the 29 known flip molecules.
**Partial:** its last words — *"the fix leaves lone-pair P centres unhandled — RDKit declines
degree-3/0-H"* — so trivalent phosphorus donors, common in this corpus, are not handled. No
before/after numbers on the 29, and the critical guard (that the descriptor still **flips for
the mirror** rather than being stable because constant) was never confirmed.

---

## The finding that reframes the release

`docs/RENUMBERING_INSTABILITY_v0.4.5.md`. On a 225-molecule seed-42 sample (223 encoded, 2
trials/transform, levers OFF):

| | count | share |
|---|---:|---:|
| byte-stable | 125/223 | 56.1% |
| drifted | 98/223 | 43.9% |
| **comparison KEY changed** | 47/223 | **21.1%** |
| **absolute stereochemistry changed** | 29/223 | **13.0%** |

`rotate` produced **zero** drift across the whole sample — the encoder is fully
orientation-invariant. Every defect is an **atom-numbering** dependence.

The 13% is a **soundness** defect: the strings are byte-identical once `@`/`@@` are deleted, so
the neighbour ordering is the same and the tags denote genuinely different configurations — at
least one of each pair is wrong. Verified by hand on `FEQFIS_comp_0` (identical sorted
coordinate multiset, a pure permutation).

**Consequence: "100% round-trip accuracy" is not currently well-defined**, because round-trip
success is partly a property of the input file's atom order. Determinism is a precondition for
measuring it.

---

## The gap to 100%, measured

Capstone arm, 6,719 molecules: passing (byte_exact + key_equal) = **93.51%**; gap = **436
molecules = 6.49%**.

| class | count | share of gap |
|---|---:|---:|
| `hard_fail` | 306 | **70.2%** (49.3% of these are timeout-shaped, ≥290 s) |
| `structural` | 70 | 16.1% |
| `encode_fail` | 48 | 11.0% |
| `facmer_divergent` | 12 | 2.8% |

Runtime: `p50 = 29.1 s · p90 = 204.9 s · p95 = 308.1 s · max = 1803 s`; only **51.1%** of
molecules already finish under 30 s.

**The speed goal and the accuracy goal are aligned** for the dominant class — 70% of the gap is
`hard_fail` and half of that is timeouts, so speed converts a timeout into a pass or a fast
informative failure.

**Honest assessment:** `encode_fail` (11% of gap) and the timeout half of `hard_fail` (~35%) are
genuinely attackable; `facmer_divergent` (2.8%) is a bounded generator-selection fix;
`structural` (16%) is a research problem — near the covalent-radius + 0.45 Å cutoff, bond
perception can return a genuinely different graph for input vs generated geometry, which the
handoff names as the deepest risk and out of scope. Realistic path: ~97–98%, with the last ~2%
requiring perception work worth scoping separately.

The largest identified unclaimed perf prize: **eta is 23.3% of molecules but 35.6% of wall
clock** and ~3× slower at equal atom count (not a size artifact — eta molecules are *smaller*).
Cause is the haptic machinery: the `scales_for_haptic` 4× sweep (`generator3d/embed.py:1094`,
`:1208`), the widened winding pool (`metallogen_adapter.py:1311`), and the C++ batch path
refusing haptic (`embed.py:1297`). Estimated upper bound ~23% off total runtime. Untouched.

---

## Resuming

1. **Finish Lane 2** (critical path; Lanes 5 and 6 are blocked on it). Start from WIP `3722b18e`,
   close `DUDREA_comp_0`, produce the A/B, confirm the over-folding guards.
2. **Finish Lane 8** — extend `stable_stereo.py` to trivalent P; prove the mirror still flips.
3. **Verify the integrated suite** — trial-merge is clean but semantically unvalidated.
4. Then Lanes 5/6, Wave D promotion A/B on the corrected instrument, the 5k sweep, perf.

Reproduce any number here with the commands in each cited doc. The canonicality instrument:
```bash
PYTHONPATH=src .venv/bin/python tools/canonicality_probe.py --n 300 --trials 2 --out <dir>
```
