# `measurements/v0.4.15` — frozen comparison artifacts

Written by `tools/harvest_measurements.py`. **Do not hand-edit** — rerun the tool.

| file | bytes | sha256 | produced by |
|---|---:|---|---|
| `attach_return_preflight.json` | 90658 | `4e5beeb4f9eabbc1` | tools/attach_return_preflight.py --sweep <frozen sweep>   (v0.4.15 Lane 1 pre-flight. Does the GUARD's predicate -- a claimed coordination SITE holding nothing, coordinate-only -- fire where `coordination_report` says DETACHED? Target structural/DETACHED 289/301 = 96.0%; exposure byte_exact/DETACHED 1/52 = 1.9%, so the charter's 52-molecule exposure over-stated it 52x; control byte_exact/INTACT 250/250 SITES_HELD, which is what makes the 1.9% a reading rather than a broken claim count) |
| `lane1_pop_L1_control_byte_exact_detached.json` | 16912 | `d162bd45fb2b58b3` | tools/run_v0415_arms.sh lane1   (v0.4.15 three-arm A/B over population L1_control_byte_exact_detached, via generator_ab_honest.py -- REAL generation, scored by re-perceiving the WRITTEN XYZ. `both` holds OIN_ATTACH_RETURN on and varies OIN_ACCEPT_STRING_EXACT, i.e. Lane 2's MARGINAL effect. ⚠ The first run of these arms was VOID: the tool's own sys.path.insert overrode PYTHONPATH and every arm imported main's oinsmiles, where neither lever exists, so both sides ran identical code and returned a flawless null. The runner now invokes the tool out of the arm's own checkout and refuses to start unless oinsmiles resolves there) |
| `lane1_pop_L1_target_site_lost.json` | 93567 | `91f549a7aa06d526` | tools/run_v0415_arms.sh lane1   (v0.4.15 three-arm A/B over population L1_target_site_lost, via generator_ab_honest.py -- REAL generation, scored by re-perceiving the WRITTEN XYZ. `both` holds OIN_ATTACH_RETURN on and varies OIN_ACCEPT_STRING_EXACT, i.e. Lane 2's MARGINAL effect. ⚠ The first run of these arms was VOID: the tool's own sys.path.insert overrode PYTHONPATH and every arm imported main's oinsmiles, where neither lever exists, so both sides ran identical code and returned a flawless null. The runner now invokes the tool out of the arm's own checkout and refuses to start unless oinsmiles resolves there) |
| `lane1_pop_control_byte_exact_intact_200.json` | 64114 | `c998debbd456c919` | tools/run_v0415_arms.sh lane1   (v0.4.15 three-arm A/B over population control_byte_exact_intact_200, via generator_ab_honest.py -- REAL generation, scored by re-perceiving the WRITTEN XYZ. `both` holds OIN_ATTACH_RETURN on and varies OIN_ACCEPT_STRING_EXACT, i.e. Lane 2's MARGINAL effect. ⚠ The first run of these arms was VOID: the tool's own sys.path.insert overrode PYTHONPATH and every arm imported main's oinsmiles, where neither lever exists, so both sides ran identical code and returned a flawless null. The runner now invokes the tool out of the arm's own checkout and refuses to start unless oinsmiles resolves there) |
| `lane2_pop_L2_mirror_match.json` | 65027 | `be6bb4b6f2c8814f` | tools/run_v0415_arms.sh lane2   (v0.4.15 three-arm A/B over population L2_mirror_match, via generator_ab_honest.py -- REAL generation, scored by re-perceiving the WRITTEN XYZ. `both` holds OIN_ATTACH_RETURN on and varies OIN_ACCEPT_STRING_EXACT, i.e. Lane 2's MARGINAL effect. ⚠ The first run of these arms was VOID: the tool's own sys.path.insert overrode PYTHONPATH and every arm imported main's oinsmiles, where neither lever exists, so both sides ran identical code and returned a flawless null. The runner now invokes the tool out of the arm's own checkout and refuses to start unless oinsmiles resolves there) |
| `lane2_pop_L2_target_key_equal.json` | 118750 | `fb68fb25ddb0a0be` | tools/run_v0415_arms.sh lane2   (v0.4.15 three-arm A/B over population L2_target_key_equal, via generator_ab_honest.py -- REAL generation, scored by re-perceiving the WRITTEN XYZ. `both` holds OIN_ATTACH_RETURN on and varies OIN_ACCEPT_STRING_EXACT, i.e. Lane 2's MARGINAL effect. ⚠ The first run of these arms was VOID: the tool's own sys.path.insert overrode PYTHONPATH and every arm imported main's oinsmiles, where neither lever exists, so both sides ran identical code and returned a flawless null. The runner now invokes the tool out of the arm's own checkout and refuses to start unless oinsmiles resolves there) |
| `lane2_pop_control_byte_exact_intact_200.json` | 64113 | `571d79aa4b6098d4` | tools/run_v0415_arms.sh lane2   (v0.4.15 three-arm A/B over population control_byte_exact_intact_200, via generator_ab_honest.py -- REAL generation, scored by re-perceiving the WRITTEN XYZ. `both` holds OIN_ATTACH_RETURN on and varies OIN_ACCEPT_STRING_EXACT, i.e. Lane 2's MARGINAL effect. ⚠ The first run of these arms was VOID: the tool's own sys.path.insert overrode PYTHONPATH and every arm imported main's oinsmiles, where neither lever exists, so both sides ran identical code and returned a flawless null. The runner now invokes the tool out of the arm's own checkout and refuses to start unless oinsmiles resolves there) |

Source paths at harvest time:

```
attach_return_preflight.json  <-  tmCAT-tmPHOTO_xyz_dataset/results-v0.4.15-preflight/attach_return_preflight.json
lane1_pop_L1_control_byte_exact_detached.json  <-  tmCAT-tmPHOTO_xyz_dataset/results-v0.4.15-arms/lane1_pop_L1_control_byte_exact_detached.json
lane1_pop_L1_target_site_lost.json  <-  tmCAT-tmPHOTO_xyz_dataset/results-v0.4.15-arms/lane1_pop_L1_target_site_lost.json
lane1_pop_control_byte_exact_intact_200.json  <-  tmCAT-tmPHOTO_xyz_dataset/results-v0.4.15-arms/lane1_pop_control_byte_exact_intact_200.json
lane2_pop_L2_mirror_match.json  <-  tmCAT-tmPHOTO_xyz_dataset/results-v0.4.15-arms/lane2_pop_L2_mirror_match.json
lane2_pop_L2_target_key_equal.json  <-  tmCAT-tmPHOTO_xyz_dataset/results-v0.4.15-arms/lane2_pop_L2_target_key_equal.json
lane2_pop_control_byte_exact_intact_200.json  <-  tmCAT-tmPHOTO_xyz_dataset/results-v0.4.15-arms/lane2_pop_control_byte_exact_intact_200.json
```
